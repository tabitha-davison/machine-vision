{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run demo",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/demo",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "lldb",
            "preLaunchTask": "build demo"
        }
    ]
}
